//Проиллюстрировать возможности логических бинарных операций.
/*#include <iostream>
#include <conio.h>
#include <Windows.h>

using namespace std;

int main()
{
 int a,b,x;
 do {
 cout << "Enter 2 numbers" << endl;
 cin >>a>>b;
 if (a%2==0 && b%2==0) cout << "The number of even-numbered";
 else {
     if (a%2==0 || b%2==0) cout << "One number is even";
     else
         cout << "The number of odd" << endl;
 }
 Sleep (1000);
 cout << endl << "Press '0' for repeat or othen digit to exit." << endl;
 cin >> x;
 }
  while (x==!1);
  cout << "Goodbye!" << endl;
 //getch();
}
*/
